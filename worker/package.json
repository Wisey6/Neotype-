{
  "name": "neotype-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that creates Stripe Checkout Sessions with a server-side price for the Neotype sticker customizer.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
